{
  "name": "rapidrecip-api",
  "version": "1.0.0",
  "description": "api desenvolvida em nodejs com mangodb",
  "main": "index.js",
  "scripts": {
    "start": "node index.js", // Adicionei o script "start" para iniciar o servidor
    "build": "echo 'Implemente o comando de compilação aqui'", // Adicionei o script "build" com uma mensagem de placeholder
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mikaias Santos",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.3.3"
  }
}
